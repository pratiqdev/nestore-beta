{
  "name": "nestore-mongo-adapter",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "keywords": [],
  "author": "pratiqdev",
  "license": "UNLICENSED",
  "description": "",
  "type":"module",
  "scripts": {
    "//& BUILD                                                  ":"",
    "//>build:lint": "Run ESLint with autofix",
    "//_CONFIGURE_LINTING_RULES - super:lint": "eslint --fix -c .eslintrc.json .",
    "//>build:clean": " remove the `./useNestore/dist/` directory",
    "build:clean-1": "rm -rf ./dist && rm -rf ../../_temp/mongoAdapter",
    "build:clean-2": "mkdir -p ../../_temp/mongoAdapter",
    "//>build:build": "built `useNestore` as a standard es module witt tsc",
    "build:build": "tsc -p ./tsconfig.json",
    "//>build:copy": "copy all relevant files to _temp/mongoAdapter",
    "build:copy-1": "cp -r ./dist ../../_temp/mongoAdapter/dist",
    "build:copy-2": "cp ./index.js ../../_temp/mongoAdapter",

    "//& TEST                                                  ":"",
    "//>test:mocha": "test the adapter with mocha",
    "test:mocha": "mocha ./test/tests --config .mocharc.cjs",

    "//& COMMANDS                                              ":"",
    "build": "npm-run-all build:*",
    "test": "npm-run-all test:*"
  },
  "devDependencies": {},
  "dependencies": {
    "@types/debug": "^4.1.7",
    "@types/lodash-es": "^4.17.6",
    "debug": "^4.3.4",
    "lodash-es": "^4.17.21",
    "mongoose": "^6.8.0",
    "nestore": "^1.4.4"
  }
}
